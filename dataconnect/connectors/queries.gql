query GetSklills($searchTerm: String ="", $limit: Int = 10) @auth(level: PUBLIC insecureReason: "Skills should be visible to everyone") {
    skills(
      where: {
        _or: [
          { title: { contains: $searchTerm } }
          { description: { contains: $searchTerm} }
        ]
      },
      orderBy: [ {
         createdAt: DESC
      }]
      limit: $limit
    ) {
      id
      title
      description
      tags
      installCommand
      createdAt
      author {
        clerkId
        username
        imageUrl
        email
      }
    }
}